name: Artifact Demo

on: push

jobs:
  build:
     runs_on: ubuntu_latest
     steps:
       - run: |
            mkdir temp
            echo "alpha" > temp/alpha.html

      - user: action/upload-artifact@v4
        with:
          name: files
          path: temp/
