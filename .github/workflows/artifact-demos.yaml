name: Artifact Demo

on: push

jobs:
  build:
     runs-on: ubuntu-latest
     steps:
       - run: |
            mkdir temp
            echo "alpha" > temp/alpha.html

      - user: actions/upload-artifact@v4
        with:
          name: files
          path: temp/
